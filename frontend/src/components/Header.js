import React from 'react';
import Logo from './Logo';

const Header = () => {
    return(
        <div className="headerContainer">
            <Logo />
        </div>
        )
}

export default Header;